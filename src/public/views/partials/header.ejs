<%- include('shared/header', { siteTitle: (typeof siteTitle !== 'undefined' ? siteTitle : 'NudeFlow'), title, headerCenter: (((title||'').toLowerCase() === 'home') ? `<div class="app-tag-status" aria-live="polite" data-tag-status>All media</div>` : '') }) %>
