<%- include('shared/header', { siteTitle: (typeof siteTitle !== 'undefined' ? siteTitle : 'NudeFlow'), title, headerCenter: (((title||'').toLowerCase() === 'home') ? `<div class="app-category-title" aria-live="polite">${(typeof currentCategoryDisplay !== 'undefined' && currentCategoryDisplay) ? currentCategoryDisplay : 'All'}</div>` : '') }) %>
